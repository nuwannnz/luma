{
	"$schema": "../../node_modules/nx/schemas/project-schema.json",
	"name": "luma-api",
	"projectType": "application",
	"root": "apps/luma-api",
	"sourceRoot": "apps/luma-api",
	"targets": {
		"build": {
			"executor": "nx:run-commands",
			"outputs": ["{projectRoot}/dist"],
			"options": {
				"cwd": "{projectRoot}",
				"command": "go build -o ./dist/api ./..."
			}
		}
	},
	"tags": []
}
